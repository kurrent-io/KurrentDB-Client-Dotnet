KURRENTDB_CLUSTER_SIZE=3
KURRENTDB_INT_TCP_PORT=1112
KURRENTDB_HTTP_PORT=2113
KURRENTDB_TRUSTED_ROOT_CERTIFICATES_PATH=/etc/kurrentdb/certs/ca
KURRENTDB_DISCOVER_VIA_DNS=false
KURRENTDB_ENABLE_ATOM_PUB_OVER_HTTP=false
KURRENTDB_STREAM_EXISTENCE_FILTER_SIZE=10000

# pass through from environment
KURRENTDB_DB_LOG_FORMAT
KURRENTDB_LOG_LEVEL
KURRENTDB_MAX_APPEND_SIZE
KURRENTDB_MEM_DB
KURRENTDB_RUN_PROJECTIONS
KURRENTDB_START_STANDARD_PROJECTIONS






# --name es-client-dotnet-test -p 2113:2113
# -e KURRENTDB_DISABLE_LOG_FILE=true
# -e KURRENTDB_ENABLE_ATOM_PUB_OVER_HTTP=true
# -e ES_DOCKER_IMAGE=docker.kurrent.io/kurrent-latest/kurrentdb:latest:latest
# -e ES_DOCKER_REGISTRY=docker.kurrent.io/kurrent-latest/kurrentdb:latest
# -e ES_USE_CLUSTER=false
# -e KURRENTDB_TRUSTED_ROOT_CERTIFICATES_PATH=/etc/eventstore/certs/ca
# -e ES_USE_EXTERNAL_SERVER=false
# -e KURRENTDB_TELEMETRY_OPTOUT=true
# -e KURRENTDB_MEM_DB=true
# -e KURRENTDB_START_STANDARD_PROJECTIONS=true
# -e KURRENTDB_ALLOW_UNKNOWN_OPTIONS=true
# -e KURRENTDB_LOG_LEVEL=Default
# -e KURRENTDB_RUN_PROJECTIONS=All
# -e ES_DOCKER_TAG=latest
# -e KURRENTDB_CERTIFICATE_FILE=/etc/eventstore/certs/node/node.crt
# -e KURRENTDB_CERTIFICATE_PRIVATE_KEY_FILE=/etc/eventstore/certs/node/node.key
# -e KURRENTDB_STREAM_EXISTENCE_FILTER_SIZE=10000
# -e KURRENTDB_STREAM_INFO_CACHE_CAPACITY=10000
# -e KURRENTDB_CHUNK_SIZE=1073741824 -e KURRENTDB_MAX_APPEND_SIZE=102400
# -e KURRENTDB_ADVERTISE_HTTP_PORT_TO_CLIENT_AS=2113 -v /Users/sergio/dev/eventstore/EventStore-Client-Dotnet/test/KurrentDB.Client.Tests/bin/Debug/net8.0/certs:/etc/kurrentdb/certs:ro
