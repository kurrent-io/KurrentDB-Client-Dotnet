name: CI

on:
  pull_request:
  push:
    branches:
      - master

jobs:
  tests:
    name: Tests
    uses: ./.github/workflows/base.yml
    strategy:
      fail-fast: false
      matrix:
        runtime: [ previous-lts, lts, ci ]
    with:
      runtime: ${{ matrix.runtime }}
    secrets: inherit
